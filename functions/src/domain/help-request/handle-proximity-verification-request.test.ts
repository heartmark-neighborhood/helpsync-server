import { MemoryHelpRequestRepository } from "../../../__test__/fake/memory-help-request.repository";

describe('近接確認結果通知の処理', () => {
  it('成功した場合は候補者の状態を更新する', () => {
  });

  it('失敗した場合は候補者の状態を更新する', () => {
    // テストの実装
  });
});
